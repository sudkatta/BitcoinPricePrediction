# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Bit coin
GET /pastPrices                controllers.Application.fetchPricesFor(range: String ?= "lastweek")

